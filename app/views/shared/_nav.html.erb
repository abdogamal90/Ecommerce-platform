<nav class="bg-white shadow-lg">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center py-4">
      <!-- Logo -->
      <%= link_to "E-Commerce App", root_path, class: "text-2xl font-bold text-gray-800" %>

      <!-- Navigation Links -->
      <div class="flex space-x-4">
        <%= link_to "Home", root_path, class: "text-gray-700 hover:text-blue-600" %>
        <%= link_to "Cart", cart_path, class: "text-gray-700 hover:text-blue-600" %>
        <% if user_signed_in? %>
          <%= link_to "Profile", edit_user_registration_path, class: "text-gray-700 hover:text-blue-600" %>
          <%= link_to "Orders", orders_path, class: "text-gray-700 hover:text-blue-600" %>
          <%= link_to "Wishlist", wishlist_items_path, class: "text-gray-700 hover:text-blue-600" %>
          <% if current_user&.admin? %>
              <%= link_to "Users", users_path, class: "text-gray-700 hover:text-blue-600" %>
          <% end %>
          <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-gray-700 hover:text-blue-600" %>
        <% else %>
          <%= link_to "Login", new_user_session_path, class: "text-gray-700 hover:text-blue-600" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "text-gray-700 hover:text-blue-600" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>